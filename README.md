# 🗺️ Social Map

Інтерактивна соціальна карта з можливістю додавання місць та подій. Користувачі можуть увійти через Google OAuth, додавати місця на карту, вказувати час дії подій та переглядати місця інших користувачів.

## ✨ Функціонал

- 🔐 Автентифікація через Google OAuth
- 📍 Додавання місць на інтерактивну карту (Leaflet)
- 👤 Відображення автора кожного місця з аватаром
- ⏰ Встановлення часу завершення подій
- 🎨 Сучасний дизайн з glassmorphism ефектами
- 🔒 Захист: користувачі можуть видаляти тільки свої місця
- 📱 Адаптивний інтерфейс

## 🚀 Швидкий старт

### Вимоги

- Node.js (v18+)
- MongoDB (через Docker або локально)
- Google OAuth credentials

### Встановлення

1. Клонуйте репозиторій:
```bash
git clone https://github.com/YOUR_USERNAME/social-map.git
cd social-map
```

2. Встановіть залежності:
```bash
# Backend
cd backend
npm install

# Frontend
cd ../frontend
npm install
```

3. Налаштуйте MongoDB:
```bash
# Запустіть MongoDB через Docker
docker-compose up -d
```

4. Налаштуйте Google OAuth:
   - Створіть проєкт у [Google Cloud Console](https://console.cloud.google.com/)
   - Отримайте Client ID та Client Secret
   - Детальні інструкції в `GOOGLE_OAUTH_INSTRUCTION.md`

5. Створіть `.env` файл в `backend/`:
```env
MONGODB_URI=mongodb://admin:admin123@localhost:27017/socialmap?authSource=admin
PORT=5000
FRONTEND_URL=http://localhost:5173
JWT_SECRET=your-secret-key
SESSION_SECRET=your-session-secret
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
```

6. Запустіть сервери:
```bash
# З кореневої папки
npm run dev

# Або окремо:
# Backend
cd backend
npm run dev

# Frontend (в іншому терміналі)
cd frontend
npm run dev
```

7. Відкрийте браузер: `http://localhost:5173`

## 📁 Структура проєкту

```
social-map/
├── backend/          # Express.js сервер
│   ├── config/       # Passport конфігурація
│   ├── controllers/  # Контролери
│   ├── models/       # Mongoose моделі
│   ├── routes/       # API роути
│   └── server.js     # Точка входу
├── frontend/         # React + Vite
│   └── src/
│       ├── components/  # React компоненти
│       ├── api.js       # API клієнт
│       └── App.jsx      # Головний компонент
└── docker-compose.yml  # MongoDB конфігурація
```

## 🛠️ Технології

### Backend
- Express.js
- MongoDB + Mongoose
- Passport.js (Google OAuth)
- Socket.io
- JWT автентифікація

### Frontend
- React 18
- Vite
- Leaflet (карти)
- React-Leaflet

## 📝 API Endpoints

- `GET /api/places` - Отримати всі місця
- `POST /api/places` - Створити місце (потрібна автентифікація)
- `GET /api/places/:id` - Отримати місце
- `PUT /api/places/:id` - Оновити місце (тільки власник)
- `DELETE /api/places/:id` - Видалити місце (тільки власник)
- `GET /api/auth/google` - Google OAuth
- `GET /api/auth/me` - Поточний користувач

## 🔒 Безпека

- JWT токени для автентифікації
- Перевірка власності при видаленні/оновленні
- Захист від CORS
- `.env` файли виключені з git

## 📄 Ліцензія

MIT

## 👤 Автор

Ваше ім'я - [GitHub](https://github.com/your-username) | [Instagram](https://instagram.com/your-username)

---

Зроблено з ❤️ використовуючи React та Node.js

