# 📋 Покрокова інструкція: Налаштування Google OAuth

## Крок 1: Відкрийте Google Cloud Console
1. Перейдіть за посиланням: https://console.cloud.google.com/
2. Увійдіть у свій Google акаунт (Gmail)

## Крок 2: Створіть новий проєкт
1. Натисніть на випадаюче меню проєктів (зверху зліва, поруч з "Google Cloud")
2. Натисніть "NEW PROJECT" (або "НОВИЙ ПРОЕКТ")
3. Введіть назву: `Social Map` (або будь-яку іншу)
4. Натисніть "CREATE" (або "СТВОРИТИ")
5. Дочекайтеся створення (10-20 секунд)
6. Виберіть новий проєкт зі списку

## Крок 3: Увімкніть Google+ API
1. У меню зліва натисніть "APIs & Services" (або "APIs і сервіси")
2. Натисніть "Library" (або "Бібліотека")
3. У пошуку введіть: `Google+ API`
4. Натисніть на "Google+ API" у результатах
5. Натисніть кнопку "ENABLE" (або "УВІМКНУТИ")
6. Дочекайтеся активації (5-10 секунд)

## Крок 4: Налаштуйте OAuth Consent Screen
1. У меню зліва натисніть "OAuth consent screen" (або "Екран згоди OAuth")
2. Виберіть "External" (або "Зовнішній")
3. Натисніть "CREATE" (або "СТВОРИТИ")

**Заповніть форму:**
- **App name** (Назва додатку): `Social Map`
- **User support email** (Email підтримки): ваш Gmail
- **Developer contact information** (Контакти розробника): ваш Gmail
- Натисніть "SAVE AND CONTINUE" (або "ЗБЕРЕГТИ І ПРОДОВЖИТИ")

**Scopes (Області доступу):**
- Натисніть "ADD OR REMOVE SCOPES" (або "ДОДАТИ АБО ВИДАЛИТИ ОБЛАСТІ")
- Знайдіть та додайте:
  - `.../auth/userinfo.email`
  - `.../auth/userinfo.profile`
- Натисніть "UPDATE" (або "ОНОВИТИ")
- Натисніть "SAVE AND CONTINUE" (або "ЗБЕРЕГТИ І ПРОДОВЖИТИ")

**Test users (Тестові користувачі):**
- Поки що пропустіть (натисніть "SAVE AND CONTINUE")

**Summary (Підсумок):**
- Перевірте інформацію
- Натисніть "BACK TO DASHBOARD" (або "ПОВЕРНУТИСЯ ДО ПАНЕЛІ")

## Крок 5: Створіть OAuth 2.0 Credentials
1. У меню зліва натисніть "Credentials" (або "Облікові дані")
2. Натисніть "CREATE CREDENTIALS" (або "СТВОРИТИ ОБЛІКОВІ ДАНІ")
3. Виберіть "OAuth client ID" (або "Ідентифікатор клієнта OAuth")

**Якщо з'явиться попередження про OAuth consent screen:**
- Натисніть "CONFIGURE CONSENT SCREEN" та виконайте Крок 4 вище

**Application type (Тип додатку):**
- Виберіть "Web application" (або "Веб-додаток")

**Name (Назва):**
- Введіть: `Social Map Backend`

**Authorized JavaScript origins (Авторизовані джерела JavaScript):**
- Натисніть "ADD URI" (або "ДОДАТИ URI")
- Введіть: `http://localhost:5000`
- Натисніть "ADD URI" ще раз
- Введіть: `http://localhost:5173` (опціонально)

**Authorized redirect URIs (Авторизовані URI перенаправлення):**
- Натисніть "ADD URI" (або "ДОДАТИ URI")
- Введіть: `http://localhost:5000/api/auth/google/callback`
- Натисніть "CREATE" (або "СТВОРИТИ")

## Крок 6: Скопіюйте Client ID та Client Secret
Після створення з'явиться вікно з:
- **Your Client ID** (Ваш ідентифікатор клієнта) - скопіюйте це!
- **Your Client Secret** (Ваш секрет клієнта) - скопіюйте це!

⚠️ **ВАЖЛИВО:** Збережіть ці дані в безпечному місці!

## Крок 7: Створіть .env файл
1. Відкрийте папку `backend/`
2. Створіть файл `.env` (без назви, тільки розширення)
3. Скопіюйте туди наступне:

```env
# MongoDB
MONGODB_URI=mongodb://admin:admin123@localhost:27017/socialmap?authSource=admin

# Server
PORT=5000
FRONTEND_URL=http://localhost:5173

# JWT Secret (замініть на будь-який випадковий рядок)
JWT_SECRET=social-map-secret-key-2024-change-in-production

# Session Secret (замініть на інший випадковий рядок)
SESSION_SECRET=social-map-session-secret-2024-change-in-production

# Google OAuth (вставте ваші дані з Кроку 6)
GOOGLE_CLIENT_ID=ваш-client-id-тут
GOOGLE_CLIENT_SECRET=ваш-client-secret-тут
```

4. Замініть `ваш-client-id-тут` та `ваш-client-secret-тут` на реальні значення з Кроку 6

## Крок 8: Готово! 🎉
Тепер запустіть сервер:
```bash
npm run dev
```

---

## ❓ Проблеми?

**Помилка "redirect_uri_mismatch":**
- Перевірте, що в Google Console точно вказано: `http://localhost:5000/api/auth/google/callback`
- Переконайтеся, що немає зайвих пробілів

**Помилка "access_denied":**
- Перевірте, що OAuth consent screen налаштовано правильно
- Переконайтеся, що додано правильні scopes

**Не працює після налаштування:**
- Перезапустіть backend сервер
- Перевірте, що файл `.env` знаходиться в папці `backend/`
- Перевірте, що всі змінні в `.env` заповнені правильно



